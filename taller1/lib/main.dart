import 'package:flutter/material.dart';

void main() {
  runApp(
      const Center(
          child: Text(
              "Hello world on flutter", textDirection: TextDirection.ltr
          )
      )
  );
}
